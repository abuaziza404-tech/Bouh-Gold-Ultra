import 'package:flutter/material.dart'; import '../../core/constants/app_identity.dart'; import '../../data/datasources/local_pack_datasource.dart'; import '../../engines/bouh_supreme_engine.dart';
class DashboardScreen extends StatelessWidget{final LocalPackDatasource pack; const DashboardScreen({super.key,required this.pack}); @override Widget build(BuildContext context){final rs=pack.cells.map(BouhSupremeEngine.analyze).toList(); final tb=rs.where((r)=>r.classLabelAr.contains('Target-B')).length; final cand=rs.where((r)=>r.classLabelAr.contains('مرشح')).length; final avg=rs.isEmpty?0:rs.map((e)=>e.ipiScore).reduce((a,b)=>a+b)/rs.length; return ListView(padding:const EdgeInsets.all(16),children:[const Text(AppIdentity.appNameAr,textAlign:TextAlign.center,style:TextStyle(fontSize:28,color:Color(0xFFD4AF37),fontWeight:FontWeight.bold)),const Text('BOUH GOLD PRO ULTRA v12.0',textAlign:TextAlign.center),const Text('تطوير وتصميم: ${AppIdentity.developerAr}',textAlign:TextAlign.center,style:TextStyle(color:Colors.white70)),const SizedBox(height:16),const Card(color:Color(0xFF101A14),child:Padding(padding:EdgeInsets.all(14),child:Text('Structure → Pattern → Alteration → Confirmation → Decision
No Structure = Reject
No Clay/SWIR = HOLD
FeOx alone = Reject'))),_card('خلايا Region Pack','${pack.cells.length}',Icons.grid_on,Colors.cyanAccent),_card('Target-B','$tb',Icons.local_fire_department,Colors.redAccent),_card('مرشحات','$cand',Icons.flag,Colors.orangeAccent),_card('متوسط IPI',avg.toStringAsFixed(1),Icons.analytics,const Color(0xFFD4AF37)),const Card(color:Color(0xFF101A14),child:Padding(padding:EdgeInsets.all(14),child:Text('تنبيه مهني: النتائج احتمالية، والقرار الاقتصادي يحتاج Field Validation + Assay + QA/QC.')))]);} Widget _card(String t,String v,IconData i,Color c)=>Card(color:const Color(0xFF101A14),child:ListTile(leading:Icon(i,color:c),title:Text(t),trailing:Text(v,style:TextStyle(color:c,fontWeight:FontWeight.bold))));}
