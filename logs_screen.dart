import 'package:flutter/material.dart'; import '../../data/datasources/field_log_datasource.dart';
class LogsScreen extends StatefulWidget{const LogsScreen({super.key}); @override State<LogsScreen> createState()=>_S();} class _S extends State<LogsScreen>{final ds=FieldLogDatasource(); @override Widget build(BuildContext context)=>FutureBuilder(future:ds.all(),builder:(context,snap){final logs=snap.data??[]; return ListView(padding:const EdgeInsets.all(16),children:[Row(children:[const Expanded(child:Text('السجل الميداني',style:TextStyle(fontSize:22,color:Color(0xFFD4AF37),fontWeight:FontWeight.bold))),IconButton(onPressed:()async{await ds.clear();setState((){});},icon:const Icon(Icons.delete))]),if(logs.isEmpty)const Card(child:Padding(padding:EdgeInsets.all(14),child:Text('لا توجد سجلات محفوظة بعد.'))),for(final l in logs) Card(color:const Color(0xFF101A14),child:ListTile(title:Text(l.decision),subtitle:Text('${l.lat.toStringAsFixed(5)}, ${l.lon.toStringAsFixed(5)}
IPI: ${l.ipi}
${l.note}',maxLines:5))) ]);});}
